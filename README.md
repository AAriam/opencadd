# Try it out

One-liner to clone the repo, checkout to development branch, install conda environment, and run the example notebook:

```
git clone https://github.com/aariam/opencadd.git && cd opencadd && git checkout v2 && conda env install && conda run --name opencadd --live-stream -vv jupyter-lab ./example.ipynb
```
